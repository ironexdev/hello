version: "3.9"

services:
  nginx:
    build:
      context: .
      dockerfile: ./etc/nginx/Dockerfile
    container_name: nginx
    depends_on:
      - php-fpm
    networks:
      - frontend
    ports:
      - 80:80
      - 443:443
    volumes:
      - ./app:/app
      - ./etc/nginx/development/ssl:/etc/ssl
      - ./etc/nginx/development/default.conf:/etc/nginx/conf.d/default.conf

  php-fpm:
    container_name: php-fpm
    build:
      context: .
      dockerfile: ./etc/php-fpm/Dockerfile
    networks:
      - backend
      - frontend
    volumes:
      - ./app:/app

  mysql:
    build:
      context: .
      dockerfile: ./etc/mysql/Dockerfile
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: "StyKmBdAMAdhfppzr6"
    networks:
      - backend
    ports:
      - 3306:3306
    volumes:
      - mysql:/var/lib/mysql

  composer:
    build:
      context: .
      dockerfile: ./etc/composer/Dockerfile
    container_name: composer
    depends_on:
      - php-fpm
    networks:
      - backend
    volumes:
      - ./app:/app

networks:
  backend:
  frontend:

volumes:
  mysql:
  testf: